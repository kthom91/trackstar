{
  "name": "browser-extension",
  "version": "1.3.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build:scripts": "node scripts/build-content-scripts.mjs",
    "build": "npm run build:scripts && ng build",
    "watch": "npm run build:scripts && ng build --watch --configuration development"
  }
}
