{
  "name": "browser-extension",
  "projectType": "application",
  "sourceRoot": "apps/browser-extension/src",
  "tags": ["type:app", "platform:browser-extension"],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "npx ng build browser-extension"
      },
      "configurations": {
        "production": {
          "command": "npx ng build browser-extension --configuration production"
        },
        "development": {
          "command": "npx ng build browser-extension --configuration development"
        }
      }
    },
    "watch": {
      "executor": "nx:run-commands",
      "options": {
        "command": "npx ng build browser-extension --watch --configuration development"
      }
    }
  }
}
